#include "mvMap.h"

MapHandler::MapHandler()
{
    return;
}

MapHandler::~MapHandler()
{
    return;
}

std::vector<MapHandler::Coordinate> MapHandler::generateNoise(
    NoiseType noiseType)
{
    // Each value will be
    std::vector<Coordinate> values;

    // generate random for x, y, z & push into values

    return values;
}